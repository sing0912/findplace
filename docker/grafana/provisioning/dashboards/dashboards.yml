apiVersion: 1

providers:
  - name: 'FindPlace Dashboards'
    orgId: 1
    folder: 'FindPlace'
    folderUid: 'findplace'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
