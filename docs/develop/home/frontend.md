# 홈 프론트엔드 UI 지침

---

## 1. 파일 구조

```
frontend/src/
├── types/home.ts                    # 홈 타입 정의
├── api/home.ts                      # 홈 API 서비스
├── hooks/
│   ├── useCustomerHome.ts           # 반려인 홈 데이터 훅
│   └── usePartnerHome.ts            # 펫시터 홈 데이터 훅
├── components/home/
│   ├── customer/
│   │   ├── ActiveBookingBanner.tsx   # 진행중 돌봄 배너
│   │   ├── RecommendedSitters.tsx    # 추천 시터 리스트
│   │   ├── RecentSitters.tsx         # 최근 이용 시터
│   │   ├── EventBanners.tsx          # 이벤트/공지 배너
│   │   └── CommunityFeeds.tsx        # 커뮤니티 피드 요약
│   └── partner/
│       ├── TodayScheduleCard.tsx     # 오늘의 일정 카드
│       ├── NewRequestBadge.tsx       # 새 예약 요청 배지
│       ├── RevenueSummaryCard.tsx    # 수익 요약 카드
│       └── NoticeList.tsx            # 공지사항 리스트
├── components/layout/
│   ├── CustomerTabBar.tsx            # 반려인 하단 탭 네비게이션
│   └── PartnerTabBar.tsx             # 펫시터 하단 탭 네비게이션
└── pages/home/
    ├── CustomerHomePage.tsx           # 반려인 홈 페이지
    └── PartnerHomePage.tsx            # 펫시터 홈 페이지
```

---

## 2. 라우팅

| 경로 | 컴포넌트 | 설명 | 역할 |
|------|----------|------|------|
| / | CustomerHomePage | 반려인 홈 | CUSTOMER |
| /partner | PartnerHomePage | 펫시터 홈 | PARTNER |

---

## 3. 반려인 홈 페이지 (CustomerHomePage)

### 3.1 레이아웃

```
┌─────────────────────────────────┐
│ PetPro        [알림벨] [설정]    │
├─────────────────────────────────┤
│                                  │
│ ┌──────────────────────────────┐│
│ │ 진행중 돌봄 배너              ││
│ │ 🐾 콩이, 두부 돌봄 중        ││
│ │ 해피독시터 | 데이케어          ││
│ │ 09:00 ~ 19:00   [바로가기 >] ││
│ └──────────────────────────────┘│
│                                  │
│ 추천 시터                [더보기]│
│ ┌────────┐ ┌────────┐ ┌────────┐│
│ │[사진]  │ │[사진]  │ │[사진]  ││
│ │해피독  │ │러블리캣│ │댕댕이집││
│ │★4.8   │ │★4.9   │ │★4.7   ││
│ │1.2km  │ │0.8km  │ │2.1km  ││
│ └────────┘ └────────┘ └────────┘│
│                                  │
│ 최근 이용 시터                   │
│ ┌──────────────────────────────┐│
│ │ [사진] 러블리캣시터  ★4.9    ││
│ │        위탁 돌봄 | 2/5 이용  ││
│ ├──────────────────────────────┤│
│ │ [사진] 해피독시터    ★4.8    ││
│ │        데이케어 | 2/1 이용   ││
│ └──────────────────────────────┘│
│                                  │
│ ┌──────────────────────────────┐│
│ │ [이벤트 배너 슬라이드]        ││
│ │ 첫 이용 30% 할인!            ││
│ │           ● ○ ○               ││
│ └──────────────────────────────┘│
│                                  │
│ 커뮤니티                 [더보기]│
│ ┌──────────────────────────────┐│
│ │ [썸네일] 소형견 데이케어 후기 ││
│ │          ♡42  💬12           ││
│ ├──────────────────────────────┤│
│ │ [썸네일] 고양이 간식 추천     ││
│ │          ♡38  💬8            ││
│ └──────────────────────────────┘│
│                                  │
├─────────────────────────────────┤
│ [홈] [검색] [예약] [채팅] [마이] │
└─────────────────────────────────┘
```

### 3.2 진행중 돌봄 배너 (ActiveBookingBanner)

| 항목 | 설명 |
|------|------|
| 표시 조건 | activeBooking이 null이 아닐 때만 표시 |
| 펫 이름 | 돌봄 대상 반려동물 이름 (콤마로 구분) |
| 시터 이름 | partnerNickname |
| 서비스 유형 | serviceTypeName |
| 시간 | startDate ~ endDate (HH:mm 형식) |
| 탭 동작 | 돌봄 진행 화면으로 이동 |
| 배경 | 밝은 파란색 그라데이션 |

### 3.3 추천 시터 리스트 (RecommendedSitters)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 가로 스크롤 카드 리스트 (최대 5명) |
| 카드 크기 | 120 x 160 |
| 카드 내용 | 프로필 이미지(60x60 원형), 닉네임, 평점(별+숫자), 거리(km) |
| 탭 동작 | 시터 상세 페이지로 이동 (/sitter/{id}) |
| 빈 상태 | "주변에 추천 시터가 없습니다" |

### 3.4 최근 이용 시터 (RecentSitters)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 세로 리스트 (최대 3명) |
| 항목 내용 | 프로필 이미지(40x40), 닉네임, 평점, 서비스 유형, 마지막 이용 날짜 |
| 탭 동작 | 시터 상세 페이지로 이동 |
| 빈 상태 | 섹션 자체를 숨김 |

### 3.5 이벤트/공지 배너 (EventBanners)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 자동 슬라이드 배너 (3초 간격) |
| 배너 내용 | imageUrl이 있으면 이미지, 없으면 텍스트 배너 |
| 탭 동작 | linkUrl로 이동 (내부 네비게이션 또는 웹뷰) |
| 인디케이터 | 하단 도트 인디케이터 |

### 3.6 커뮤니티 피드 요약 (CommunityFeeds)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 세로 리스트 (최대 3개) |
| 항목 내용 | 썸네일(40x40), 제목, 카테고리, 좋아요 수, 댓글 수 |
| 탭 동작 | 커뮤니티 게시글 상세로 이동 |
| "더보기" 버튼 | 커뮤니티 메인 페이지로 이동 |

---

## 4. 펫시터 홈 페이지 (PartnerHomePage)

### 4.1 레이아웃

```
┌─────────────────────────────────┐
│ PetPro 파트너     [알림벨]       │
├─────────────────────────────────┤
│                                  │
│ 오늘의 일정 (3건)                │
│ ┌──────────────────────────────┐│
│ │ 09:00~19:00  데이케어         ││
│ │ 김** | 콩이(말티즈)  [확정]   ││
│ ├──────────────────────────────┤│
│ │ 10:00~18:00  데이케어         ││
│ │ 이** | 두부(푸들)    [진행중] ││
│ ├──────────────────────────────┤│
│ │ 14:00~15:00  산책             ││
│ │ 박** | 초코(비글)    [확정]   ││
│ └──────────────────────────────┘│
│                                  │
│ ┌──────────────────────────────┐│
│ │  새 예약 요청  2건    [확인>] ││
│ └──────────────────────────────┘│
│                                  │
│ 이번 달 수익                     │
│ ┌──────────────────────────────┐│
│ │ 확정 수익     1,250,000원     ││
│ │ 정산 대기       350,000원     ││
│ │ 완료 건수           15건      ││
│ └──────────────────────────────┘│
│                                  │
│ 공지사항                 [더보기]│
│ ┌──────────────────────────────┐│
│ │ 2월 정산 일정 안내     02.06  ││
│ │ 시터 가이드라인 업데이트 02.03 ││
│ │ 앱 업데이트 안내       02.01  ││
│ └──────────────────────────────┘│
│                                  │
├─────────────────────────────────┤
│ [홈] [일정] [예약관리] [채팅] [마이]│
└─────────────────────────────────┘
```

### 4.2 오늘의 일정 카드 (TodayScheduleCard)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 세로 리스트 (오늘의 전체 건) |
| 항목 내용 | 시간(HH:mm~HH:mm), 서비스 유형, 반려인 이름(마스킹), 펫 이름(견종), 상태 뱃지 |
| 상태 뱃지 | CONFIRMED: 파란색 "확정", IN_PROGRESS: 녹색 "진행중" |
| 탭 동작 | 예약 상세로 이동 |
| 빈 상태 | "오늘 예정된 일정이 없습니다" |

### 4.3 새 예약 요청 배지 (NewRequestBadge)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 강조 카드 (밝은 주황색 배경) |
| 내용 | "새 예약 요청 N건" |
| 탭 동작 | 예약 관리 > 요청 탭으로 이동 |
| 빈 상태 | 0건이면 "새 예약 요청이 없습니다" (회색 배경) |

### 4.4 수익 요약 카드 (RevenueSummaryCard)

| 항목 | 설명 |
|------|------|
| 확정 수익 | totalAmount (원 단위, 천단위 콤마) |
| 정산 대기 | pendingAmount |
| 완료 건수 | completedBookingCount |
| 탭 동작 | 마이 > 정산/수익 페이지로 이동 |

### 4.5 공지사항 리스트 (NoticeList)

| 항목 | 설명 |
|------|------|
| 표시 형태 | 간단한 텍스트 리스트 (최대 3개) |
| 항목 내용 | 제목, 날짜 (MM.dd 형식) |
| 탭 동작 | 공지사항 상세로 이동 |
| "더보기" | 전체 공지사항 리스트로 이동 |

---

## 5. 하단 탭 네비게이션

### 5.1 반려인 탭 (CustomerTabBar)

| 순서 | 아이콘 | 라벨 | 경로 |
|------|--------|------|------|
| 1 | home | 홈 | / |
| 2 | search | 검색 | /search |
| 3 | calendar | 예약 | /bookings |
| 4 | chat | 채팅 | /chat |
| 5 | user | 마이 | /my |

### 5.2 펫시터 탭 (PartnerTabBar)

| 순서 | 아이콘 | 라벨 | 경로 |
|------|--------|------|------|
| 1 | home | 홈 | /partner |
| 2 | calendar | 일정 | /partner/calendar |
| 3 | clipboard | 예약관리 | /partner/bookings |
| 4 | chat | 채팅 | /partner/chat |
| 5 | user | 마이 | /partner/my |

### 5.3 탭 규칙

- 활성 탭: 아이콘 및 라벨 색상 변경 (프라이머리 컬러)
- 비활성 탭: 회색
- 채팅 탭: 읽지 않은 메시지가 있으면 뱃지(빨간 점) 표시
- 예약 요청(시터): 미응답 요청이 있으면 뱃지(숫자) 표시

---

## 6. 타입 정의 (types/home.ts)

```typescript
// 반려인 홈 응답
interface CustomerHomeResponse {
  activeBooking: ActiveBooking | null;
  recommendedSitters: RecommendedSitter[];
  recentSitters: RecentSitter[];
  eventBanners: EventBanner[];
  communityFeeds: CommunityFeed[];
}

interface ActiveBooking {
  bookingId: number;
  bookingNumber: string;
  partnerNickname: string;
  partnerProfileImageUrl: string | null;
  serviceType: string;
  serviceTypeName: string;
  startDate: string;
  endDate: string;
  status: string;
  petNames: string[];
}

interface RecommendedSitter {
  id: number;
  nickname: string;
  profileImageUrl: string | null;
  address: string;
  distance: number | null;
  averageRating: number;
  reviewCount: number;
  completedBookingCount: number;
  services: SitterServiceSummary[];
  acceptablePetTypes: string[];
}

interface SitterServiceSummary {
  serviceType: string;
  serviceTypeName: string;
  basePrice: number;
}

interface RecentSitter {
  id: number;
  nickname: string;
  profileImageUrl: string | null;
  averageRating: number;
  lastBookingDate: string;
  serviceType: string;
  serviceTypeName: string;
}

interface EventBanner {
  id: number;
  title: string;
  imageUrl: string | null;
  linkUrl: string;
  type: 'EVENT' | 'NOTICE';
}

interface CommunityFeed {
  id: number;
  title: string;
  category: string;
  categoryName: string;
  likeCount: number;
  commentCount: number;
  thumbnailUrl: string | null;
  createdAt: string;
}

// 펫시터 홈 응답
interface PartnerHomeResponse {
  todaySchedule: TodaySchedule;
  newRequestCount: number;
  revenueSummary: RevenueSummary;
  notices: NoticeItem[];
}

interface TodaySchedule {
  date: string;
  totalCount: number;
  bookings: TodayBooking[];
}

interface TodayBooking {
  bookingId: number;
  bookingNumber: string;
  customerName: string;
  serviceType: string;
  serviceTypeName: string;
  startDate: string;
  endDate: string;
  status: string;
  petNames: string[];
  petCount: number;
}

interface RevenueSummary {
  month: string;
  totalAmount: number;
  completedBookingCount: number;
  pendingAmount: number;
}

interface NoticeItem {
  id: number;
  title: string;
  createdAt: string;
}
```

---

## 7. API 서비스 (api/home.ts)

```typescript
const homeApi = {
  getCustomerHome: (params?: { latitude?: number; longitude?: number }) =>
    client.get<CustomerHomeResponse>('/api/v1/home/customer', { params }),

  getPartnerHome: () =>
    client.get<PartnerHomeResponse>('/api/v1/home/partner'),
};
```

---

## 8. 커스텀 훅

```typescript
// useCustomerHome.ts
const { data, loading, error, refetch } = useCustomerHome(latitude, longitude);

// usePartnerHome.ts
const { data, loading, error, refetch } = usePartnerHome();
```

### 8.1 데이터 갱신 전략

| 이벤트 | 동작 |
|--------|------|
| 앱 포그라운드 진입 | 자동 refetch |
| Pull to Refresh | 수동 refetch |
| 탭 전환으로 홈 복귀 | 마지막 fetch로부터 5분 경과 시 refetch |

---

## 9. 상태 처리

| 상태 | 반려인 홈 | 펫시터 홈 |
|------|-----------|-----------|
| 로딩 | 각 섹션별 스켈레톤 UI | 각 카드별 스켈레톤 UI |
| 에러 | "홈 화면을 불러올 수 없습니다" + 재시도 버튼 | 동일 |
| 빈 상태 | 섹션별로 개별 처리 (위 참조) | 섹션별로 개별 처리 (위 참조) |
