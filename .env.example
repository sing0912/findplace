# ============================================================
# PetPro - 환경 변수 설정 (예시)
# ============================================================
# 이 파일을 복사하여 .env 파일로 사용하세요.
# cp .env.example .env
# 반드시 모든 your-* 값을 실제 값으로 변경하세요.

# Database (PostgreSQL)
DB_NAME=petpro
DB_USERNAME=petpro
DB_PASSWORD=your-secure-db-password

# Database Ports
DB_MASTER_PORT=5432
DB_SLAVE1_PORT=5433
DB_SLAVE2_PORT=5434

# Replication
REPLICATOR_PASSWORD=your-replicator-password

# Coupon Database
COUPON_DB_PASSWORD=your-coupon-db-password

# Log Database (MySQL)
LOG_DB_USERNAME=loguser
LOG_DB_PASSWORD=your-log-db-password
LOG_DB_ROOT_PASSWORD=your-log-db-root-password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# MinIO (S3 Compatible)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=your-minio-access-key
MINIO_SECRET_KEY=your-minio-secret-key
MINIO_BUCKET=petpro

# JWT
JWT_SECRET=your-jwt-secret-key-must-be-at-least-256-bits-long
JWT_ACCESS_EXPIRATION=3600
JWT_REFRESH_EXPIRATION=1209600

# Grafana
GRAFANA_ADMIN_PASSWORD=your-grafana-password

# Application
APP_PORT=8080
APP_ENV=local

# Frontend
FRONTEND_URL=http://localhost:3000

# Google Maps
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# GitHub (배포용)
GITHUB_REPO=sing0912/findplace

# OAuth (선택)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
